import { GetHtmlEntityFromSuitPipe } from './get-html-entity-from-suit.pipe';

describe('GetHtmlEntityFromSuitPipe', () => {
  it('create an instance', () => {
    const pipe = new GetHtmlEntityFromSuitPipe();
    expect(pipe).toBeTruthy();
  });
});
